const initialState = {
    isFetching: false,
    brewery: null,
    location: null,
    error: ""
  };

  export const brewReducer = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  }; 
